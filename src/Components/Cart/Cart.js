import React from 'react';

const Cart = (props) => {
    console.log(props)
    // const {items} = props.items;
    // console.log(items.length);
    return (
        <div>
           <h1>click items : {}</h1>
        </div>
    );
};

export default Cart;